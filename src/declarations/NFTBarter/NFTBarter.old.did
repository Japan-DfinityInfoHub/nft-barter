type UserProfile = variant {none;};
type UserId = principal;
type Result_1 = 
 variant {
   err: text;
   ok: UserProfile;
 };
type Result = 
 variant {
   err: text;
   ok: UserId;
 };
service : {
  getMyInfo: () -> (Result_1) query;
  isRegistered: () -> (bool) query;
  register: () -> (Result);
}
