type UserProfile = variant {none;};
type Result = 
 variant {
   err: text;
   ok: UserProfile;
 };
service : {
  getMyProfile: () -> (Result) query;
  isRegistered: () -> (bool) query;
  register: () -> (Result);
}
