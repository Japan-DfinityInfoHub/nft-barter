type UserId__1 = principal;
type UserId = principal;
type Result_1 = 
 variant {
   err: text;
   ok: DefiniteUser;
 };
type Result = 
 variant {
   err: text;
   ok: UserId;
 };
type DefiniteUser = 
 record {
   id: UserId__1;
   name: text;
 };
service : {
  getMyInfo: () -> (Result_1) query;
  isRegistered: () -> (bool) query;
  register: () -> (Result);
}
